name: tarteel_qul_example
description: A minimal runnable demo of the tarteel_qul mushaf engine.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  tarteel_qul:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
