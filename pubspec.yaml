name: quran_player
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

workspace:
  - packages/quran_mcp_server
  - packages/tarteel_qul
  - packages/tarteel_qul/example

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # UI library — see .claude/skills/forui/SKILL.md for project conventions.
  # Bumped to ^0.21 alongside the Flutter 3.41 / Dart 3.11 upgrade
  # (carried by the mushaf-reader change to unblock qcf_quran_plus).
  forui: ^0.21.3

  # State management.
  flutter_riverpod: ^2.6.1

  # Declarative routing.
  go_router: ^14.6.2

  # Logging facade (see lib/core/logging/logger.dart).
  logging: ^1.3.0

  # Persists ThemeMode + future user preferences to OS-specific storage.
  shared_preferences: ^2.3.4

  # Quran corpus storage (read-only, bundled SQLite asset).
  # Desktop-only `sqflite` flavor; the build tool uses `sqlite3` directly.
  sqflite_common_ffi: ^2.3.3
  path_provider: ^2.1.5
  path: ^1.9.0

  # SHA-256 for runtime integrity verification of the bundled DB.
  crypto: ^3.0.6

  http: ^1.6.0
  media_kit: ^1.2.6
  media_kit_libs_audio: ^1.0.7

  # Workspace member: loopback MCP server. Imports only pure Dart contracts;
  # the host app provides the Quran/audio/auditlog/scope-check adapters at
  # composition time. See packages/quran_mcp_server/ and AGENTS.md > Lib layout.
  quran_mcp_server:
    path: packages/quran_mcp_server

  # Workspace member: the QUL printed-mushaf rendering engine. Page mode
  # renders through this package; only lib/data/quran/mushaf_locator_qul.dart
  # and lib/features/reader/widgets/page_mushaf_view.dart import it — every
  # other layer reaches the printed mushaf via the MushafLocator seam.
  tarteel_qul:
    path: packages/tarteel_qul

  # Unzips per-page mushaf fonts on demand from the bundled assets/qul/ttf.zip.
  # Used only by the host MushafAssetSource in lib/data/quran/.
  archive: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Maintainer-only build tool (`tool/build_quran_db.dart`). Not shipped at runtime.
  sqlite3: ^2.4.6

  # Used by test/workspace_member_test.dart to parse the root pubspec.yaml and
  # assert the workspace declaration. Test-only — available transitively at
  # runtime, but imported explicitly here for test analyzer clarity.
  yaml: ^3.1.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/quran/quran.sqlite
    - assets/quran/manifest.json
    - assets/tafsir/muyassar.sqlite
    - assets/tafsir/manifest.json
    # QUL mushaf rendering data — page mode renders through these. NOT in the
    # repo (assets/qul/ is gitignored); a contributor downloads them per the
    # README setup step. A fresh clone cannot build until the download runs.
    - assets/qul/qpc-v4-tajweed-15-lines.db
    - assets/qul/qpc-v4.db
    - assets/qul/ttf.zip
    # QUL ornamental surah-header (COLR) + quran-common fonts — render the
    # reader's surah_name / basmallah lines and the Surahs-list names.
    - assets/qul/surah_headers/QCF_SurahHeader_COLOR-Regular.ttf
    - assets/qul/juz_name_font/quran-common.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
